vile:
  ignore:
    - appveyor.yml
    - bin
    - circle.yml
    - CODE_OF_CONDUCT.md
    - coffeelint.json
    - coverage
    - .editorconfig
    - .git
    - .gitattributes
    - .gitignore
    - LICENSE
    - node_modules
    - .nyc_output
    - package-lock.json
    - README.md
    - src/@types
    - tags
    - test/fixtures/**
    - tsconfig.json
    - tslint.json
    - tmp
    - .travis.yml
    - retire-report.json
    - .vile.yml

depcheck:
  config:
    ignore_dev_deps: true
    ignore_deps:
      - "@types/**"
    ignore_dirs:
      - src
      - node_modules
      - tmp

eclint:
  ignore:
    - lib

retire:
  config:
    ignore:
      - lib

comment:
  ignore:
    - lib

escomplex:
  allow:
    - lib
  ignore:
    - src

coverage:
  config:
    path: coverage/lcov.info

language:
  ignore:
    - lib

coffeelint:
  config: coffeelint.json
